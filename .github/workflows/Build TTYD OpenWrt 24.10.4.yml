name: 为 RK3568 构建带 HTTPS (TLS) 的 TTYD，使用 OpenWrt 24.10.4

on:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH 连接到 Actions'
        required: false
        default: 'false'

permissions:
  contents: write
  packages: read

env:
  TZ: Asia/Shanghai
  # 使用 OpenWrt 24.10.4 SDK for Rockchip ARMv8
  SDK_URL: https://downloads.openwrt.org/releases/24.10.4/targets/rockchip/armv8/openwrt-sdk-24.10.4-rockchip-armv8_gcc-13.3.0_musl.Linux-x86_64.tar.zst

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - name: 检出代码库
        uses: actions/checkout@v4

      - name: 安装编译依赖
        run: |
          sudo -E rm -rf /usr/share/dotnet /etc/mysql /etc/php /usr/local/lib/android
          sudo apt update -y
          sudo apt full-upgrade -y
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext \
          genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev \
          libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev \
          libreadline-dev libssl-dev libtool llvm lrzsz libnsl-dev ninja-build p7zip p7zip-full patch pkgconf \
          python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion \
          swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev

      - name: 下载并解压 OpenWrt 24.10.4 SDK
        run: |
          wget -q ${{ env.SDK_URL }}
          file_name=$(echo ${{ env.SDK_URL }} | awk -F/ '{print $NF}')
          mkdir sdk
          if [[ $file_name == *.tar.xz ]]; then
            tar -xJf $file_name -C ./sdk --strip-components=1
          elif [[ $file_name == *.tar.zst ]]; then
            tar --zstd -x -f $file_name -C ./sdk --strip-components=1
          else
            echo "不支持的文件格式: $file_name"
            exit 1
          fi
          echo "SDK_DIR=$PWD/sdk" >> $GITHUB_ENV

      - name: 彻底清理官方 libwebsockets 包
        run: |
          cd $SDK_DIR
          echo "=== 开始彻底清理官方 libwebsockets 包 ==="
          
          # 首先更新 feeds 获取包列表
          ./scripts/feeds update -a
          
          # 使用 feeds 命令卸载官方包
          ./scripts/feeds uninstall libwebsockets || true
          
          # 彻底删除所有官方 libwebsockets 相关文件
          echo "移除 feeds/packages/libs/libwebsockets..."
          rm -rf feeds/packages/libs/libwebsockets
          
          echo "移除 package/feeds/packages/libwebsockets..."
          rm -rf package/feeds/packages/libwebsockets
          
          echo "搜索并移除其他位置的 libwebsockets 包..."
          find . -type d -name "libwebsockets" ! -path "*/libwebsockets-full/*" ! -path "*/dl/*" -exec echo "移除: {}" \; -exec rm -rf {} + || true
          
          # 清理构建缓存
          echo "清理构建缓存..."
          rm -rf build_dir/target-*/libwebsockets-* || true
          rm -rf build_dir/target-*/libwebsockets || true
          rm -rf staging_dir/target-*/pkginfo/libwebsockets* || true

      - name: 彻底清理官方 luci-app-ttyd 包
        run: |
          cd $SDK_DIR
          echo "=== 开始彻底清理官方 luci-app-ttyd 包 ==="
          
          # 使用 feeds 命令卸载官方包
          ./scripts/feeds uninstall luci-app-ttyd || true
          
          # 彻底删除所有官方 luci-app-ttyd 相关文件
          echo "移除 feeds/luci/applications/luci-app-ttyd..."
          rm -rf feeds/luci/applications/luci-app-ttyd
          
          echo "移除 package/feeds/luci/luci-app-ttyd..."
          rm -rf package/feeds/luci/luci-app-ttyd
          
          echo "搜索并移除其他位置的 luci-app-ttyd 包..."
          find . -type d -name "luci-app-ttyd" ! -path "*/luci-app-ttyd/*" ! -path "*/dl/*" -exec echo "移除: {}" \; -exec rm -rf {} + || true
          
          # 清理构建缓存
          echo "清理构建缓存..."
          rm -rf build_dir/target-*/luci-app-ttyd-* || true
          rm -rf build_dir/target-*/luci-app-ttyd || true
          rm -rf staging_dir/target-*/pkginfo/luci-app-ttyd* || true

      - name: 复制自定义软件包到 SDK
        run: |
          # 检查包目录是否存在
          if [ -d "libwebsockets-full" ]; then
            cp -r libwebsockets-full $SDK_DIR/package/
            echo "✅ libwebsockets-full 包已复制"
          else
            echo "❌ libwebsockets-full 目录未找到"
            exit 1
          fi
          
          if [ -d "ttyd" ]; then
            cp -r ttyd $SDK_DIR/package/
            echo "✅ ttyd 包已复制"
          else
            echo "❌ ttyd 目录未找到"
            exit 1
          fi
          
          # 复制 luci-app-ttyd 包
          if [ -d "luci-app-ttyd" ]; then
            cp -r luci-app-ttyd $SDK_DIR/package/
            echo "✅ luci-app-ttyd 包已复制"
          else
            echo "❌ luci-app-ttyd 目录未找到"
            exit 1
          fi

      - name: SSH 连接到 Actions (调试)
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event.inputs.ssh == 'true' }}

      - name: 安装 feeds 并验证清理
        run: |
          cd $SDK_DIR
          echo "=== 安装 feeds（不包含官方包）==="
          ./scripts/feeds install -a
          
          echo "=== 验证官方包是否已彻底删除 ==="
          # 验证 libwebsockets
          if [ -d "feeds/packages/libs/libwebsockets" ]; then
            echo "❌ 错误：feeds/packages/libs/libwebsockets 仍然存在"
            exit 1
          fi
          
          if [ -d "package/feeds/packages/libwebsockets" ]; then
            echo "❌ 错误：package/feeds/packages/libwebsockets 仍然存在"
            exit 1
          fi
          
          # 验证 luci-app-ttyd
          if [ -d "feeds/luci/applications/luci-app-ttyd" ]; then
            echo "❌ 错误：feeds/luci/applications/luci-app-ttyd 仍然存在"
            exit 1
          fi
          
          if [ -d "package/feeds/luci/luci-app-ttyd" ]; then
            echo "❌ 错误：package/feeds/luci/luci-app-ttyd 仍然存在"
            exit 1
          fi
          
          echo "✅ 所有官方包已彻底删除"
          echo "✅ 自定义包状态："
          find package -name "*libwebsockets*" -type d
          find package -name "*luci-app-ttyd*" -type d

      - name: 配置 OpenWrt 构建
        run: |
          cd $SDK_DIR
          echo "=== 配置构建目标 ==="
          # 配置为 RK3568 目标
          echo "CONFIG_TARGET_rockchip=y" > .config
          echo "CONFIG_TARGET_rockchip_armv8=y" >> .config
          echo "CONFIG_TARGET_rockchip_armv8_DEVICE_friendlyarm_nanopi-r5s=y" >> .config
          
          # 选择 libuv 和自定义的 libwebsockets-full
          echo "CONFIG_PACKAGE_libuv=y" >> .config
          echo "CONFIG_PACKAGE_libwebsockets-full=m" >> .config
          echo "CONFIG_PACKAGE_ttyd=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-ttyd=m" >> .config
          
          # 明确禁用其他 libwebsockets 变体
          echo "# CONFIG_PACKAGE_libwebsockets is not set" >> .config
          echo "# CONFIG_PACKAGE_libwebsockets-openssl is not set" >> .config
          echo "# CONFIG_PACKAGE_libwebsockets-mbedtls is not set" >> .config
          
          echo "=== 应用默认配置 ==="
          make defconfig
          
          echo "=== 最终配置检查 ==="
          echo "libuv 和 libwebsockets 相关配置:"
          grep -E "CONFIG_PACKAGE_(libuv|libwebsockets)" .config || echo "未找到相关配置"

      - name: 下载软件包源码
        run: |
          cd $SDK_DIR
          # 首先下载基础包和自定义包
          echo "下载 libuv 源码..."
          make package/feeds/packages/libuv/download V=s
          
          if [ -d "package/libwebsockets-full" ]; then
            echo "下载 libwebsockets-full 源码..."
            make package/libwebsockets-full/download V=s
          fi
          
          make package/ttyd/download V=s
          make package/luci-app-ttyd/download V=s
          
          echo "检查下载的文件:"
          find dl -size -1024c -exec ls -l {} \; || true

      - name: 编译 libuv
        id: compile_libuv
        run: |
          cd $SDK_DIR
          echo "🚀 开始编译 libuv..."
          
          make package/feeds/packages/libuv/clean V=s
          
          if make package/feeds/packages/libuv/compile -j$(nproc) V=s; then
            echo "status=success" >> $GITHUB_OUTPUT
            echo "✅ libuv 编译成功"
            
            # 验证编译产物
            echo "=== 检查 libuv 编译产物 ==="
            find bin -name "*libuv*" -type f | head -10 || echo "未找到 libuv 产物"
          else
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ libuv 编译失败"
            exit 1
          fi

      - name: 编译 libwebsockets-full
        id: compile_libwebsockets
        run: |
          cd $SDK_DIR
          echo "🚀 开始编译 libwebsockets-full..."
          
          # 首先清理可能的旧构建
          make package/libwebsockets-full/clean V=s
          
          if make package/libwebsockets-full/compile -j$(nproc) V=s; then
            echo "status=success" >> $GITHUB_OUTPUT
            echo "✅ libwebsockets-full 编译成功"
            
            # 验证编译产物
            echo "=== 检查编译产物 ==="
            find bin -name "*libwebsockets*" -type f | head -10 || echo "未找到 libwebsockets 产物"
          else
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ libwebsockets-full 编译失败"
            exit 1
          fi

      - name: 编译 ttyd
        id: compile_ttyd
        run: |
          cd $SDK_DIR
          echo "🚀 开始编译 ttyd..."
          make package/ttyd/clean V=s
          
          if make package/ttyd/compile -j$(nproc) V=s; then
            echo "status=success" >> $GITHUB_OUTPUT
            echo "✅ ttyd 编译成功"
          else
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ ttyd 编译失败"
            exit 1
          fi

      - name: 编译 luci-app-ttyd
        id: compile_luci_app_ttyd
        run: |
          cd $SDK_DIR
          echo "🚀 开始编译 luci-app-ttyd..."
          make package/luci-app-ttyd/clean V=s
          if make package/luci-app-ttyd/compile -j$(nproc) V=s; then
            echo "status=success" >> $GITHUB_OUTPUT
            echo "✅ luci-app-ttyd 编译成功"
          else
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ luci-app-ttyd 编译失败"
            exit 1
          fi
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV

      - name: 收集构建产物
        if: steps.compile_libuv.outputs.status == 'success' && steps.compile_libwebsockets.outputs.status == 'success' && steps.compile_ttyd.outputs.status == 'success' && steps.compile_luci_app_ttyd.outputs.status == 'success'
        run: |
          cd $SDK_DIR
          mkdir -p upload
          
          # 查找所有相关的 IPK 文件
          echo "=== 搜索 IPK 文件 ==="
          
          # 查找 libuv IPK 文件
          find bin -name "*libuv*.ipk" -type f | while read file; do
            echo "找到 libuv IPK: $file"
            cp "$file" upload/
          done
          
          find bin -name "*ttyd*.ipk" -type f | while read file; do
            echo "找到 ttyd IPK: $file"
            cp "$file" upload/
          done
          
          find bin -name "*libwebsockets*.ipk" -type f | while read file; do
            echo "找到 libwebsockets IPK: $file"
            cp "$file" upload/
          done
          
          # 查找 luci-app-ttyd IPK 文件
          find bin -name "*luci-app-ttyd*.ipk" -type f | while read file; do
            echo "找到 luci-app-ttyd IPK: $file"
            cp "$file" upload/
          done
          
          # 查找二进制文件
          echo "=== 搜索二进制文件 ==="
          find build_dir -name "ttyd" -type f | while read file; do
            if file "$file" | grep -q "ELF"; then
              echo "找到 ELF 二进制文件: $file"
              cp "$file" upload/ttyd
              chmod +x upload/ttyd
              break
            fi
          done
          
          echo "📁 上传目录内容:"
          ls -la upload/

      - name: 生成发布信息
        id: release_info
        if: steps.compile_libuv.outputs.status == 'success' && steps.compile_libwebsockets.outputs.status == 'success' && steps.compile_ttyd.outputs.status == 'success' && steps.compile_luci_app_ttyd.outputs.status == 'success'
        run: |
          cd $SDK_DIR
          # 获取当前日期时间
          BUILD_DATE=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')
          
          # 获取 libuv 版本信息
          LIBUV_VERSION=$(find bin -name "*libuv*.ipk" -type f | head -1 | grep -o 'libuv[^_]*' | sed 's/libuv[^0-9]*//' || echo "未知版本")
          
          cat > upload/RELEASE_INFO.md << EOF
          ## 🌐 适用于 RK3568 的 TTYD (HTTPS/TLS) 构建 - OpenWrt 24.10.4
          
          ### 构建信息
          - **构建日期**: $BUILD_DATE
          - **OpenWrt 版本**: 24.10.4
          - **目标平台**: Rockchip ARMv8 (RK3568)
          - **代码库**: ${{ github.repository }}
          - **提交**: ${{ github.sha }}
          - **libuv 版本**: $LIBUV_VERSION
          - **libwebsockets**: 自定义完整版（完全替换官方版本）
          
          ### 包含的软件包
          - **libuv** - 异步 I/O 库，提供事件循环支持
          - **ttyd** - 通过网络共享您的终端，支持 TLS/SSL
          - **libwebsockets-full** - 完整的 WebSocket 库，支持 OpenSSL（替换官方版本）
          - **luci-app-ttyd** - ttyd 的 LuCI 网页界面（自定义版本）
          
          ### 安装说明
          1. 将 IPK 文件上传到您的 RK3568 OpenWrt 24.10.4 设备
          2. 安装软件包 (按顺序安装):
          \`\`\`bash
          # 首先安装 libuv 依赖
          opkg install libuv_*.ipk
          
          # 然后安装自定义的 libwebsockets-full
          opkg install libwebsockets-full_*.ipk
          
          # 最后安装 ttyd 和界面
          opkg install ttyd_*.ipk
          opkg install luci-app-ttyd_*.ipk
          \`\`\`
          
          3. 启用服务:
          \`\`\`bash
          /etc/init.d/ttyd enable
          /etc/init.d/ttyd start
          \`\`\`
          
          4. 配置 TTYD 使用 SSL (如果 LuCI 界面未自动配置):
          \`\`\`bash
          # 编辑 ttyd 配置
          uci set ttyd.@ttyd[0].ssl='1'
          uci set ttyd.@ttyd[0].ssl_cert='/etc/uhttpd.crt'
          uci set ttyd.@ttyd[0].ssl_key='/etc/uhttpd.key'
          uci commit ttyd
          /etc/init.d/ttyd restart
          \`\`\`
          
          5. 或者使用系统已有的证书 (如果存在):
          \`\`\`bash
          # 检查系统证书
          ls -la /etc/ssl/certs/
          
          # 使用现有证书启动 ttyd
          ttyd --ssl --ssl-cert /path/to/cert.crt --ssl-key /path/to/cert.key -p 7681 bash
          \`\`\`
          
          6. 通过浏览器访问: \`https://您的设备IP:7681\`
          
          ### 重要说明
          - 此构建包含兼容的 libuv 库，解决 "failed to load evlib_uv" 错误
          - 使用自定义的 libwebsockets-full 包，完全替换了官方版本
          - 使用自定义的 luci-app-ttyd 包，完全替换了官方版本
          - 所有软件包均针对 OpenWrt 24.10.4 和 RK3568 架构优化编译
          
          ### 文件列表
          EOF
          
          echo '```' >> upload/RELEASE_INFO.md
          ls -la upload/ >> upload/RELEASE_INFO.md
          echo '```' >> upload/RELEASE_INFO.md
          
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 生成包含日期和随机数的唯一标签
        id: tag_generator
        if: steps.compile_libuv.outputs.status == 'success' && steps.compile_libwebsockets.outputs.status == 'success' && steps.compile_ttyd.outputs.status == 'success' && steps.compile_luci_app_ttyd.outputs.status == 'success' && steps.release_info.outputs.status == 'success'
        run: |
          # 生成随机四位数
          RANDOM_NUM=$(shuf -i 1000-9999 -n 1)
          # 构建标签：OpenWrt-ttyd-年月日-随机四位数
          BUILD_TAG="OpenWrt-ttyd-$(date +%Y%m%d)-${RANDOM_NUM}"
          echo "生成的标签: $BUILD_TAG"
          echo "tag_name=$BUILD_TAG" >> $GITHUB_OUTPUT

      - name: 创建 GitHub 发布
        if: steps.tag_generator.outputs.tag_name
        run: |
          echo "使用标签创建发布: ${{ steps.tag_generator.outputs.tag_name }}"
          gh release create "${{ steps.tag_generator.outputs.tag_name }}" \
            --title "OpenWrt 24.10.4 TTYD HTTPS 构建 for RK3568 $(date +'%Y-%m-%d')" \
            --notes-file $SDK_DIR/upload/RELEASE_INFO.md \
            $SDK_DIR/upload/*.ipk \
            $SDK_DIR/upload/ttyd
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 上传产物到 GitHub
        uses: actions/upload-artifact@v4
        with:
          name: ttyd-https-packages-24.10.4
          path: |
            $SDK_DIR/upload/*.ipk
            $SDK_DIR/upload/ttyd
          if-no-files-found: warn
